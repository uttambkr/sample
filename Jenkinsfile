pipeline {
   agent {label 'cloud' }
   stages {
       stage('example') {
           steps {
               script {
                  echo "${JENKINS_HOME}"
               }
           }
       }
   }
}
 
